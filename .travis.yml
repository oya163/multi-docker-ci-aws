sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t oyashi/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true oyashi/react-test npm test

after_success:
  - docker build -t oyashi/multi-client ./client
  - docker build -t oyashi/multi-nginx ./nginx
  - docker build -t oyashi/multi-server ./server
  - docker build -t oyashi/multi-worker ./worker

  # Login to the docker CLI
  # Pass the docker password as standar input to docker login command
  - echo "$DOCKER_PASSWORD" | docker login --username "$DOCKER_ID" --password-stdin

  # Take those images and push them to docker hub
  - docker push oyashi/multi-client
  - docker push oyashi/multi-nginx
  - docker push oyashi/multi-server
  - docker push oyashi/multi-worker
